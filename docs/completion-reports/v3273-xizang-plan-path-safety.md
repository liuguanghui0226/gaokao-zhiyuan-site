# v3.273 西藏 2026 计划路径安全分流完成报告

## 结论

西藏 2026 官方全量计划此前已经完成入库，本轮没有重复导入。运行分片保留 7,300 条 `official-xizang-admission-plan-2026` 专业级计划；普通推荐新增限定资格路径安全分流。

全来源中有 1,109 条计划被识别为部队生源、对口高职、专项、预科、提前军警/艺体、提前录取或定向资格路径。物理/理科画像范围内为 527 条；它们不再进入普通自动推荐。普通物理/理科画像仍有 2,519 条普通计划候选，工程兴趣画像可返回 3 个计划层专业选项。

## 保持的边界

- 招生计划只说明官方可选专业池，不能生成录取概率、最低分或最低位次。
- 普通批中的警校、消防等名称不作为单独排除条件，避免仅凭名称误删正常专业；只按官方批次、标签和明确资格限制分流。
- 西藏仍缺本地可计算的一分一段，普通录取数据与专业投档边界未闭合时，推荐继续降级并提示人工复核。

## 验证

```text
node --check site/assets/app.js
node scripts/test-recommendation-boundaries-v3273.mjs
node scripts/test-recommendation-boundaries-v3272.mjs
node scripts/test-browser-runtime-shards-v3272.mjs
```

全部通过。HTTP 回读确认西藏分片由 `mac_2T-mirror` 提供；静态 `app.js` 与内部 APFS 源文件哈希一致。

## 浏览器限制

本机内置浏览器运行在独立网络沙箱，不能连接 shell 中的 `127.0.0.1:4177`。未修改任何 VPN、代理、路由或 DNS；真实页面逻辑由 Node VM 契约测试和 HTTP 静态回读覆盖，本地服务仍在 `http://127.0.0.1:4177/` 可供本机浏览器打开。
